---
type Props = { date: Date };

const { date } = Astro.props;
---

<time class="text-xs" datetime={date.toISOString()}>
  {
    date.toLocaleDateString("pt-BR", {
      year: "numeric",
      month: "long",
      day: "numeric",
    })
  }
</time>
